// const readline = require("readline");
// const fs = require("fs");
// const inform = console.log;

// const rl = readline.createInterface({
//   input: process.stdin,
//   output: process.stdout,
// });

// /*
// rl.question('Enter the file path: ', (filePath) => {
//   // Check if the file exists
//   if (fs.existsSync(filePath)) {
//     // File exists, do something with it
//     console.log(`Selected file: ${filePath}`);
//     // Additional processing logic here
//   } else {
//     // File does not exist
//     console.error('File does not exist');
//   }

//   rl.close();
// });

// In this example, we use the readline module to create an interface for input and output using process.stdin and process.stdout. The rl.question function prompts the user to enter the file path. The entered file path is then captured in the filePath variable.

// After capturing the file path, you can perform additional operations on the file, such as reading its content or processing it as needed. The example provided simply logs the selected file path to the console.

// Make sure to install the required modules by running npm install readline before executing the JavaScript file using Node.js.

// */
// export default async function buildNewGif(srcType) {
//   inform("\nthe source type is:\n" + srcType);
// }
